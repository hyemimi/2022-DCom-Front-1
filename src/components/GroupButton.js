import React, { useEffect, useState, useRef } from 'react'
function GroupButton({text}) {
    return(
        <div className="out">
            <button className="groups-btn">{text}</button>
        </div>
  )
}

export default GroupButton
